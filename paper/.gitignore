# Build artifacts. The LaTeX source is the deliverable; main.pdf is committed
# alongside it so the paper can be read without a TeX installation, and is
# rebuilt from main.tex rather than edited.
*.aux
*.log
*.out
*.synctex.gz
